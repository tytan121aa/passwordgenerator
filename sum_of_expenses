plik = input("Podaj nazwę pliku:")
with open(plik) as stream:
    costs = stream.read()
costs = costs.split("\n")

total = 0
for cost in costs:
    tokens = cost.split()
    if tokens:
        expense = tokens[0]
        total += float(expense)

print("Suma wydatków: ", total)
